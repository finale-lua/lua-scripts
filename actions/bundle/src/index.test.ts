import { helloWorld } from './index'

it('should output "hello world"', () => {
    expect(helloWorld()).toEqual('hello world')
})

it('should output "hello world"', () => {
    expect(helloWorld()).toEqual('hello world')
})

it('should output "hello world"', () => {
    expect(helloWorld()).toEqual('hello world')
})
