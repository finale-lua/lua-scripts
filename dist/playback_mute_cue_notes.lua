function plugindef()finaleplugin.RequireSelection=true;finaleplugin.Author="Nick Mazuk"finaleplugin.Copyright="CC0 https://creativecommons.org/publicdomain/zero/1.0/"finaleplugin.Version="1.0"finaleplugin.Date="June 19, 2020"finaleplugin.CategoryTags="Playback"finaleplugin.AuthorURL="https://nickmazuk.com"return"Mute Cue Notes","Mute Cue Notes","Mutes notes that are 85% normal size or smaller"end;function playback_cues_mute()local a=85;for b in eachentrysaved(finenv.Region())do local c=false;local d=finale.FCNoteheadMod()if b:CalcResize()>a then for e in each(b)do d:LoadAt(e)if d.Resize>a then c=true end end end;b.Playback=c end end;playback_cues_mute()