name: 'Bundle scripts'
description: 'Greet someone and record the time'
inputs:
  source:
    description: 'The directory of the source files'
    required: true
  output:
    description: 'The directory of the outputted bundled files'
    required: true
runs:
  using: 'node20'
  main: 'dist/index.js'